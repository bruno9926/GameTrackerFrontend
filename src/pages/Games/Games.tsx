import AnimatedRoute from "../AnimatedRoute";
// hooks
import useGames from "../../hooks/useGames";

const Games = () => {
  return (
    <AnimatedRoute>
      <h1>Games</h1>
      {}
    </AnimatedRoute>
  );
};

export default Games;
